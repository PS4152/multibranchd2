pipeline {
    agent any
    environment {
        //key value pairs
        name = "sumanth"
        course ="k8s"
    }
    stages {
        stage ("build") {
            steps {
                echo "welcome ${name}"
                echo "you enrolled to ${course} course"
                //echo "you are certified in gcp"
            }
        }
    }
}
